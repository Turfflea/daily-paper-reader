---
title: "Neuro-Relational Programs: Unifying Queries and Neural Computation over Structured Data"
title_zh: 神经关系程序：统一结构化数据上的查询与神经计算
authors: "Arie Soeteman, Balder ten Cate, Maurice Funk, Benny Kimelfeld, Carsten Lutz, Moritz Schönherr"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11946v1"
tags: ["query:mt"]
score: 9.0
evidence: 通过神经关系程序统一图神经计算与关系数据库查询
tldr: 传统深度学习方法处理关系数据时缺乏统一的声明式框架，难以融合符号推理与嵌入学习。神经关系程序(NRP)扩展Datalog，关联元组与向量嵌入，并通过嵌入组合、聚合等操作在同一语言中实现关系与神经计算。研究表明，NRP片段可恢复GNN、深度同态网络等架构，且完整NRP的表达力等同于带计数一阶逻辑FOCQ，对应均匀TC^0类。NRP建立了数据库查询与神经架构之间的桥梁，为结构化数据的深度学习提供了理论统一。
source: arxiv
selection_source: fresh_fetch
motivation: 处理关系数据的深度学习方法常需在图表示与查询间切换，缺乏一体化的声明式编程模型。
method: 提出神经关系程序(NRP)，扩展Datalog，在元组上附加嵌入，并提供组合、聚合、变换操作，统一查询与神经计算。
result: 分析NRP语法片段，恢复GNN、深度同态网络等；完整NRP等价于FOCQ，对应均匀TC^0，建立逻辑与计算类联系。
conclusion: NRP统一了关系数据上的查询与神经计算，为神经网络设计提供声明式框架，连接数据库理论与深度学习。
---

## 摘要
对关系数据库进行深度学习的传统方法是将图神经网络等神经模型应用于数据库的图表示。近期方法则直接在数据库上操作，将元组与嵌入相关联，并扩展查询机制以共同处理嵌入和关系内容。受这些进展启发，我们引入神经关系程序（NRP），这是一种面向关系数据库的声明式查询语言，其事实带有数值向量嵌入。NRP 通过组合、聚合和转换嵌入的操作来扩展数据记录风格的规则，从而在单一形式体系中交织关系推理和可学习的神经组件。这为关系数据上的神经计算提供了一种通用方法：一个 NRP 既可解读为带有可训练组件的查询计划，也可解读为内置关系结构的神经架构。NRP 的自然语法片段能复现已有的架构和查询形式体系。零目 NRP 对应于非自适应查询算法；单目 NRP 推广了图神经网络式的消息传递，并精确捕获了深度同态网络，我们将这一联系扩展到带行标识的数据库上的前沿守护 NRP。我们通过 FOCQ——在实值加权结构上解释的带计数的一阶逻辑扩展——刻画了具有 ReLU-FFN 变换的无限制 NRP 的表达能力，从而建立了与有序数据库上的一致 TC⁰ 之间的精确联系。总之，这些结果确立了 NRP 作为关系数据上查询与神经计算的广泛声明式框架的地位。

## Abstract
The conventional approach to deep learning over relational databases applies neural models, such as Graph Neural Networks (GNNs), to a graph representation of the database. Recent approaches instead operate on databases directly, associating tuples with embeddings and extending query mechanisms to jointly process embeddings and relational content. Inspired by these developments, we introduce Neuro-Relational Programs (NRPs), a declarative query language for relational databases whose facts carry numeric vector embeddings. NRPs extend Datalog-style rules with operations that combine, aggregate, and transform embeddings, thereby interleaving relational reasoning and learnable neural components within a single formalism. This yields a general approach to neural computation over relational data: an NRP can be read both as a query plan with trainable components and as a neural architecture with relational structure built in.   Natural syntactic fragments of NRPs recover existing architectures and query formalisms. Zero-ary NRPs correspond to non-adaptive query algorithms; monadic NRPs generalize GNN-style message passing and precisely capture Deep Homomorphism Networks, a connection that we extend to frontier-guarded NRPs over databases with row-ids. We characterize the expressive power of unrestricted NRPs with ReLU-FFN transformations by FOCQ, an extension of first-order logic with counting interpreted over real-weighted structures, yielding a precise connection with uniform TC$^0$ over ordered databases. Together, these results establish NRPs as a broad declarative framework for querying and neural computation over relational data.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **问题背景**：传统方法在关系数据库上应用深度学习时，需先将数据转为图表示，再用图神经网络（GNN）处理。这种“数据库→图转换→GNN”的双范式设计割裂了关系查询与神经计算，开发流程复杂。
- **研究动机**：若能直接在关系数据库上进行神经计算，将元组与嵌入绑定，并在查询过程中完成嵌入的传播与变换，就能实现声明式的统一编程模型，简化开发并增强表达力。
- **整体含义**：提出一个声明式框架——神经关系程序（NRP），将关系代数/日志规则与可学习的神经组件（如聚合、组合、变换）融合在一起，使同一个程序既是查询计划又是神经架构，为关系数据的神经计算建立理论桥梁。

## 2. 论文提出的方法论

- **核心思想**：扩展Datalog风格的规则，让每条事实携带实数向量嵌入。通过三类规则在关系运算中同步操作嵌入，实现关系推理与神经计算的交织。
- **关键技术细节**：
    - **数据模型**：嵌入式数据库，每个事实 `R(c)⟨e⟩` 包含内容元组和嵌入向量，嵌入维度固定。
    - **规则类型**：
        1. **合取规则（Conjunction）**：`R(x)⟨SUM⟩ ⇐ ⊙ R₁(x₁) ∧ ... ∧ Rℓ(xℓ)`。对于满足变量的所有匹配，将各事实的嵌入用逐元素乘积（⊙）组合，再按头元组分组求和聚合。
        2. **析取规则（Disjunction）**：`R(x)⟨SUM⟩ ⇐ R₁(x) ∨ ... ∨ Rℓ(x)`。合并多个关系中相同内容元组的嵌入并求和。
        3. **变换规则（Transformation）**：`R(x)⟨μ⟩ ⇐ R₀(x)`。对每个元组的嵌入应用可微函数（如ReLU-FFN）进行变换。
    - **聚合与组合的固定选择**：默认聚合函数为逐元素求和（空和为0向量），组合函数为适应不等长向量的逐元素乘积。通过重写可证明求和（sum）与拼接（⊕）不增加表达力。
    - **语法片段**：零目NRP（所有IDB无内容元数）、单目NRP（IDB至多1内容元数）、前沿守护NRP（规则头部变量被某个体原子覆盖）。这些片段对应不同能力层级。
    - **查询定义**：可将NRP与接受策略结合，实现最终布尔/平展查询。
- **表达力刻画**：
    - 零目NRP = 非自适应查询算法（homomorphism counts）。
    - 单目NRP = 深度同态网络（DHN），且能超越GNN+同态计数特征。
    - 完整NRP（ReLU-FFN变换） = 逻辑FOCQ（带实值求和的一阶逻辑），在有序经典数据库上捕获一致`TC⁰`复杂度类。

## 3. 实验设计

- **数据类型与基准**：本文为纯理论论文，未设计或执行具体的实验评估。
- **对比方法**：文中未报告实验对比。理论比较的对象包括：非自适应查询算法、图神经网络（GNN）、深度同态网络（DHN）、逻辑表达式FOCQ/FO+C等，通过定理证明关系。
- **外部系统**：作者提及有一个伴随的系统实现（引用[38]），可将NRP编译为PyTorch/cuDF/SQL的物理计划，并声称性能可比目前最优实现，但本文未展示实验数据。

## 4. 资源与算力

- 论文中没有给出任何具体算力资源信息（如GPU型号、数量、训练时间等）。
- 作者明确指出本文聚焦于前向传播表达力分析，而NRP框架天然支持端到端梯度训练，其参数存在于变换函数中，但本文未进行训练实验。

## 5. 实验数量与充分性

- 本文未做实验，因此不适用实验数量与充分性的评价。论文的贡献全部在于形式化定义、定理证明和理论等价性。
- 客观公平性：理论证明结果（如等价性、包含关系）是严格且客观的，无需实验验证。

## 6. 论文的主要结论与发现

1. **统一框架**：NRP成功将关系查询和神经计算融合在一个声明式语言中，同一程序可作为查询计划和神经架构。
2. **语法片段与现有架构的对应**：
    - 零目NRP精确刻画非自适应查询算法。
    - 单目NRP精确刻画深度同态网络（DHN），且严格强于GNN和基于同态计数的增强GNN。
    - 前沿守护NRP在行标识归一化数据库下可转换为单目NRP→DHN。
3. **全集NRP的复杂性刻画**：具有ReLU-FFN变换的NRP与逻辑FOCQ等价，表达力严格包含于一致TC⁰；在有序经典数据库上恰好捕获一致TC⁰。
4. **逻辑与系统的连接**：NRP为神经-关系学习提供了坚实的理论基础，连接了数据库理论、有限模型论和并行计算复杂度。

## 7. 优点

- **理论深度与统一性**：从非自适应查询到GNN/DHN，再到并行复杂度类，用统一框架给出了优雅的层级刻画。
- **声明式简洁性**：仅用三类规则（合取、析取、变换）和少数固定操作（和积组合、求和聚合）完成表达，设计简洁。
- **实际可用性潜力**：NRP可直接对应查询计划，支持梯度训练，且已有原型系统将其编译成高效物理执行。
- **严谨的等价证明**：对零目、单目和全集程序提供了“当且仅当”的表达力定理，无歧义。

## 8. 不足与局限

- **缺乏实验验证**：作为理论论文，未提供任何经验结果（如训练效率、准确率、扩展性），实际表现尚未在本工作中证明。
- **变换函数假设较强**：理论核心结果（FOCQ、TC⁰）基于ReLU-FFN和有理参数假设，对于其他激活函数或更复杂的神经模块表达力未讨论。
- **面向“经典”数据库有限制**：对于有序条件、行标识归一化、嵌入维度固定等约束，可能限制了在真实系统部署中的灵活性。
- **优化与训练未覆盖**：前向表达力分析不包含反向传播、参数共享、优化稳定性等实际深度学习挑战。
- **接受策略的简单性**：文中使用简单的门控接受策略（如`>0`），对于更复杂的决策边界可能需要扩展。

（完）
