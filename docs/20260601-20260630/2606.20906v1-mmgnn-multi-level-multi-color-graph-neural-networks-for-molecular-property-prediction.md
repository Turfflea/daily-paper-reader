---
title: "MMGNN: Multi-level, multi-color graph neural networks for molecular property prediction"
title_zh: MMGNN：面向分子性质预测的多层级多颜色图神经网络
authors: "Trung Nguyen, Duc Duy Nguyen"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20906v1"
tags: ["query:mt"]
score: 9.0
evidence: 分层多色图神经网络将分子图分解为子图进行性质预测。
tldr: 传统分子图神经网络用单图传播多样化学交互，导致特异性信号混合，且需深层网络捕捉长程效应。MMGNN将分子图分解为重叠的、按原子类型对着色的子图，2D变体利用共价键，3D变体利用空间邻近并引入距离、角度、扭转等几何描述符，各子图共享消息传递骨干，最终聚合得到分子表征。在MoleculeNet的五个分类和三个回归任务上，MMGNN-2D获得最高分类宏平均AUC-ROC 0.838和ESOL最低RMSE 0.803，MMGNN-3D在BBBP上AUC-ROC达0.956、FreeSolv RMSE为1.793，展示出拓扑与几何特征的互补优势。该重叠交互特异的图分解为分子性质预测提供了高效且具竞争力的策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20906v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1556, \"height\": 1176, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20906v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1333, \"height\": 619, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20906v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1576, \"height\": 1173, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20906v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1576, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20906v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1572, \"height\": 643, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20906v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1102, \"height\": 585, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20906v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1723, \"height\": 587, \"label\": \"Table\"}]"
motivation: 传统分子消息传递网络用单一图混合化学多样交互，易淹没特异性信号并需深层传播捕获长程效应。
method: MMGNN将分子图按原子类型对分解为重叠着色子图，2D用共价键，3D用空间邻近并附加距离、角度、扭转描述符，各子图共享消息传递后原子聚合读出。
result: 在MoleculeNet上，MMGNN-2D分类宏平均AUC-ROC最高0.838，ESOL RMSE最低0.803；MMGNN-3D在BBBP AUC-ROC 0.956、FreeSolv RMSE 1.793最优，表明拓扑与几何互补。
conclusion: 子图分解影响学习表征和原子对敏感性，拓扑与几何优势互补，重叠交互特异的图分解是分子性质预测的有竞争力方法。
---

## 摘要
分子消息传递神经网络通常通过单一图传播化学上多样的相互作用，这可能混合了特定于相互作用的信号，并需要深层传播来捕捉长程效应。我们提出了多层级多颜色图神经网络（MMGNN），这是一个层次化框架，将分子图分解为重叠的原子类型对特定子图，同时保持原子级别的分辨率。MMGNN-2D从共价连接构建化学着色的子图，而MMGNN-3D从空间邻近性构建几何着色的子图，并用距离、角度和二面角描述符增强其边。两种变体都对每个子图应用共享的通信消息传递骨干网，并通过原子级聚合和分子读出组合得到的表示。我们在MoleculeNet的五个分类和三个回归基准上评估了MMGNN，使用常见的骨架划分和五次独立运行。MMGNN-2D在分类数据集上取得了最高的宏平均AUC-ROC（0.838）以及在ESOL上最低的RMSE（0.803）。MMGNN-3D在BBBP上获得了最高的平均AUC-ROC（0.956）并在FreeSolv上获得了最低的RMSE（1.793），表明了拓扑和几何表示的互补优势。结构分析和留一分析进一步说明了子图分解如何影响学习到的表示和原子类型对的敏感性。这些结果支持将重叠的特定相互作用图分解作为分子性质预测的一种有竞争力的策略。

## Abstract
Molecular message-passing neural networks commonly propagate chemically diverse interactions through a single graph, which may mix interaction-specific signals and require deep propagation to capture long-range effects. We introduce the Multi-level, Multi-color Graph Neural Network (MMGNN), a hierarchical framework that decomposes a molecular graph into overlapping atom-type-pair-specific subgraphs while preserving atom-level resolution. MMGNN-2D constructs chemical-colored subgraphs from covalent connectivity, whereas MMGNN-3D constructs geometric-colored subgraphs from spatial proximity and augments their edges with distance, angular, and torsional descriptors. Both variants apply a shared communicative message-passing backbone to each subgraph and combine the resulting representations through atom-wise aggregation and molecular readout. We evaluated MMGNN on five classification and three regression benchmarks from MoleculeNet using common scaffold splits and five independent runs. MMGNN-2D achieved the highest macro-average AUC-ROC of 0.838 across the classification datasets and the lowest RMSE on ESOL (0.803). MMGNN-3D obtained the highest mean AUC-ROC on BBBP (0.956) and the lowest RMSE on FreeSolv (1.793), indicating complementary strengths of topological and geometric representations. Structural and leave-one-out analyses further illustrate how the subgraph decomposition affects learned representations and atom-type-pair sensitivities. These results support overlapping interaction-specific graph decomposition as a competitive strategy for molecular property prediction.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：传统的分子消息传递神经网络（MPNNs）通常在一个单一的分子图上聚合所有化学相互作用。这种做法存在两个主要缺陷：
    *   **信号混合**：不同类型的化学相互作用（如共价键、非共价键等）在同一个消息传递通道中被混合，可能导致特定于相互作用的化学信号被稀释或掩盖。
    *   **长程依赖困难**：为了捕捉长程相互作用，网络需要深度传播，但这会引发过度平滑或过度挤压等问题，导致信息衰减。同时，远距离的原子在2D拓扑上不相邻，但在3D空间上可能很近，单一化学图无法直接表达这种关系。

*   **整体含义**：为了解决上述问题，本文提出了多层级多颜色图神经网络（MMGNN）。其核心思想是“分而治之”：将复杂的分子图分解为一系列重叠的、特定于原子类型对的子图，在子图内独立进行消息传递，再进行聚合。这旨在**延迟**不同化学信号的混合，更早地捕捉特定于局部环境的模式，同时保持原子级别的分辨率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

*   **核心思想**：MMGNN 的架构核心是**重叠的、特定于原子类型对的子图分解**。分子不被视为一个单一图，而是一组子图的集合，每个子图（称为“着色子图”）只包含特定类型的原子对（如 C-C 子图、C-O 子图等）。

*   **两种互补变体**：
    *   **MMGNN-2D（化学着色子图）**：基于**共价键连接**构建子图。它专注于学习由化学拓扑结构决定的局部信息。
    *   **MMGNN-3D（几何着色子图）**：基于**空间邻近性**构建子图。它根据原子间的欧氏距离（引入一个上下界来过滤无关或过近的原子对）来创建边，从而直接捕捉长程的非共价空间相互作用。其边特征还丰富了多体几何描述符：2体距离（高斯径向基函数展开）、3体角度（圆形基函数展开）和4体二面角（傅里叶级数展开）。

*   **算法流程（三阶段）**：
    1.  **分子图构建**：为整个分子定义一组原子类型。对于每一对原子类型，构建一个对应的子图。例如，`G_{C-N}` 子图包含所有碳原子和氮原子，以及它们之间的有效边（在2D中为共价键，在3D中为满足距离条件的空间边）。
    2.  **并行子图消息传递**：对每个构建好的子图，使用一个共享的**通信消息传递神经网络（CMPNN）**骨干网独立地进行消息传递。CMPNN 使用一种邻居流机制和有向边更新：
        *   对于子图内的有向边 `i → j`，其初始表示 `h_{i→j}` 由原子特征 `X_i` 和边特征 `X_{ij}` 拼接转换而来。
        *   在第 *t* 步，聚合节点 *i* 所有入边的消息，并减去反向边 `j → i` 的旧消息，以避免信息回流。
        *   更新有向边状态 `h_{i→j}^{(t)}`，该过程结合了新的邻居消息和初始残差信息。
        *   经过 *T* 步后，每个原子的子图级表示由其出边的最终状态聚合而成。
    3.  **子图聚合与性质预测**：由于子图是重叠的，同一个原子 `i` 可能出现在多个子图中，拥有多个子图级表示 `h_{i,sub}^{(s)}`。通过**原子级聚合**（如求和、均值或注意力机制）将这些表示融合，得到原子的最终表示 `h_i`。之后，所有原子的最终表示通过**全局求和池化**得到整个分子的表示 `h_{mol}`，并用一个前馈神经网络进行最终的分类或回归预测。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

*   **数据集**：从 MoleculeNet 基准测试中选择了 **8个数据集**，涵盖：
    *   **分类任务（5个）**：BBBP（血脑屏障渗透性）、BACE、ClinTox、SIDER、Tox21。
    *   **回归任务（3个）**：ESOL（溶解度）、FreeSolv（水合自由能）、Lipophilicity（脂溶性）。

*   **数据划分与评估**：采用**骨架划分**（Scaffold Split，8:1:1比例）来评估模型的泛化能力。性能报告为**5次独立运行的平均值和标准差**。所有对比的基线模型均使用相同的划分和随机种子重新训练，保证了公平性。
    *   分类指标：AUC-ROC。
    *   回归指标：RMSE。

*   **Baseline 方法对比**：对比了 **11种** 主流图神经网络方法，覆盖了不同的范式，包括：GCN、GAT、Weave、SchNet、MPNN、D-MPNN、CMPNN、AttentiveFP、CoMPT、CD-MVGNN 和 GSL-MPP。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力

*   **算力说明**：论文**未明确提及**所使用的 GPU 型号、数量以及模型的训练时长。这是文章在实验细节描述上的一个缺失。

### 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平

*   **实验数量**：实验设计较为全面，包含以下部分：
    *   **主要性能对比**：8个数据集 × 2个模型变体 vs 11个基线方法。
    *   **消融研究（在支撑信息中提及）**：关于不同聚合策略（求和、均值、注意力）的详细对比。
    *   **可解释性分析 1 - 结构案例研究**：选取一对共享骨架但性质相反的对映体，通过可视化隐藏表示的余弦相似度，对比MMGNN与CMPNN在区分局部化学信号上的能力。
    *   **可解释性分析 2 - 子图重要性分析**：在所有8个数据集上，对2D和3D变体均进行了留一子图（Leave-One-Out Subgraph）消融实验，以评估每种特定于原子类型对的子图对预测性能的影响。

*   **充分性与客观性**：实验是充分、客观且公平的。
    *   **充分性**：覆盖了分类和回归任务，并通过可解释性实验深入探究了模型的工作原理。
    *   **客观与公平**：使用了标准的骨架划分检验泛化能力，所有对比方法在同一条件下重新运行，结果以均值和标准差报告，排除了随机性和数据划分差异带来的干扰。

### 6. 论文的主要结论与发现

*   **性能有竞争力**：MMGNN在分子性质预测任务上具有竞争力。MMGNN-2D 在5个分类数据集上取得了最高的宏平均 AUC-ROC（0.838），并在 ESOL 上取得了最低的 RMSE（0.803）。MMGNN-3D 在 BBBP（0.956）和 FreeSolv（1.793）上表现最优。
*   **拓扑与几何信息互补**：MMGNN-2D 和 MMGNN-3D 在不同任务上的表现优势，证明了基于共价键的拓扑信息和基于空间的3D几何信息对分子性质预测具有**互补价值**。例如，3D 信息对预测血脑屏障渗透和溶解自由能特别有益。
*   **子图分解的有效性**：
    *   **结构案例研究**表明，相比于全局消息传递，重叠子图分解能更好地**保留局部的、功能特异性的化学信号**，避免被大型共享骨架稀释，从而做出更准确的预测。
    *   **留一子图分析**表明，模型学习到的重要性模式与化学直觉相符。2D模型更依赖于碳骨架等拓扑关系，而3D模型对O-O、N-N等空间上接近的极性杂原子对更为敏感。

### 7. 优点：方法或实验设计上有哪些亮点

*   **新颖的图分解范式**：提出了“重叠的、交互特异的子图”这一核心概念，不同于传统的图粗化或碎片方法，它既保留了原子级别的细节，又为不同化学环境创建了隔离的消息传递通道，有效延缓了信号混合。
*   **统一的拓扑与几何框架**：设计了2D和3D变体，共享相同的核心架构（CMPNN+聚合），这使得可以**控制变量**，直接对比研究化学拓扑与几何信息对各项分子性质的不同贡献。
*   **全面的可解释性分析**：实验不仅限于性能对比，还通过结构案例和系统性的留一子图消融实验，深入揭示了模型学到的是什么，为模型行为提供了具有化学意义的解释。
*   **严格的实验对比**：对所有基线模型使用相同的骨架划分和随机种子重新训练，保证了性能对比的公平性和结论的可靠性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **计算开销未明确**：论文未提及模型的参数规模、训练/推理时间等计算成本。将分子分解为多个子图并分别处理，可能会带来比标准GNN更高的计算复杂度，但这一点没有得到量化讨论。
*   **超参数与组件消融**：缺乏对关键超参数（如3D几何图的距离截断值 `dcut`）以及3D几何特征（距离、角度、二面角）各自贡献的消融研究（文中仅提及支撑信息中有聚合策略的消融）。CMPNN骨干网的选择对性能的贡献也未与更简单的消息传递网络进行对比消融。
*   **任务领域局限**：所有实验均基于 MoleculeNet 的小分子数据集。该框架在更复杂的分子体系（如蛋白质-配体复合物、大分子）及其他非药物性质预测任务（如材料科学）上的表现尚未得到验证。
*   **依赖性风险**：3D几何子图的构建依赖于原子准确的3D坐标，这通常需要昂贵的计算或实验获取。在大量缺乏3D结构的分子数据集上，MMGNN-3D的应用会受到限制。

（完）
