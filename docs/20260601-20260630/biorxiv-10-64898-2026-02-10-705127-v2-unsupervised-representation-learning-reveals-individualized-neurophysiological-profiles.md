---
title: Unsupervised Representation Learning Reveals Individualized Neurophysiological Profiles
title_zh: 无监督表示学习揭示个体化神经生理特征
authors: "Lapatrie, M., da Silva Castanheira, J., Aydin, I., Baillet, S."
date: 2026-06-26
pdf: "https://www.biorxiv.org/content/10.64898/2026.02.10.705127v2.full.pdf"
tags: ["query:mt"]
score: 9.0
evidence: 使用无条件的自编码器进行无监督表示学习，从MEG数据中学习神经生理特征，无需标签，符合自监督学习。
tldr: "人脑活动包含稳定个体特异性神经生理特征，但现有监督方法可能利用标签中的偶然特异信息。本研究提出无需参与者标签的自编码器框架，以重建为目标从静息态MEG中学习隐空间。所得特征在120秒内个体识别准确率达93.3%，仅14秒数据即可超越基线，跨会话仍高于随机，且能预测年龄。该无监督方法为可扩展、可解释的神经生理学特征分析提供了新途径。"
source: biorxiv
selection_source: fresh_fetch
motivation: 监督式个体神经生理特征分析方法可能依赖于标签中的偶然特异性，无法确保反映稳定生物学差异。
method: 提出参与者无关的自编码器，用静息态MEG重建任务学习隐空间特征，无需个体标签。
result: "无监督特征在120秒内达93.3%识别准确率，14秒即超基线，跨会话泛化49.5%，年龄预测r2=0.318。"
conclusion: 无监督表示学习可有效提取个体神经生理特征，为可扩展和可解释的脑电分析提供新框架。
---

## 摘要
人类大脑活动包含稳定、个体特异的特征，可持续数月至数年，形成神经生理特征。大多数基于模型的特征分析方法使用参与者标签或监督目标，使得难以确定成功的区分反映的是稳定的生物学差异还是可利用的特异性。我们引入了一个与参与者无关的自编码器框架，该框架仅以重建作为训练目标，从短暂的静息态脑磁图(MEG)片段中提取特征。在没有参与者标签的情况下，从学习的潜在空间中涌现出判别性特征。在同一会话内，自编码器特征在120秒时达到93.3%的准确率，当源重建中不包含参与者特定解剖信息时，在短至14秒的记录中，其性能超过功能连接、频谱和对比基线方法。该区分能力在不同记录会话间泛化至高于随机水平(预训练自编码器的会话间准确率为49.5%)。这些特征在预测年龄方面也比基线更准确(r²=0.318)，解码器使得在频谱和连接空间中进行基于扰动的敏感性分析成为可能。这确立了与参与者无关的表示学习是一种可扩展且可解释的特征分析方法。

## Abstract
Human brain activity contains stable, individual-specific features that persist over months to years, forming neurophysiological profiles. Most model-based profiling approaches use participant labels or supervised objectives, making it difficult to determine whether successful differentiation reflects stable biology or exploitable idiosyncrasies. We introduce a participant-agnostic autoencoder framework that derives profiles from brief resting-state magnetoencephalography (MEG) segments using reconstruction as sole training objective. Discriminative profiles emerged from the learned latent space without participant labels. Within-session, autoencoder profiles reached 93.3% accuracy at 120 s, exceeding functional-connectivity, spectral, and contrastive baselines with recordings as short as 14 s when participant-specific anatomy was withheld from source reconstruction. Differentiation generalized above chance across recording sessions (between-session accuracy 49.5% for the pretrained autoencoder). Profiles also predicted age more accurately than baselines (r2=0.318), and the decoder enabled perturbation-based sensitivity analyses in spectral and connectivity spaces. This establishes participant-agnostic representation learning as a scalable and interpretable profiling.

---

## 论文详细总结（自动生成）

好的，以下是基于所提供论文内容生成的结构化中文总结。

### 1. 核心问题与整体含义

-   **研究背景与动机**：
    -   已知人脑静息态活动包含稳定、个体特异的“神经生理特征”，可用于个体识别（即“大脑指纹”）。
    -   现有方法主要分为两类：一是基于预定义特征（如功能连接、功率谱）的“无模型”方法，虽具可解释性但受限于特征空间；二是使用深度学习的“模型化”方法，但其通常依赖**参与者标签**进行监督或对比学习，以最大化个体间差异为目标。
    -   **核心问题**：依赖标签的监督目标可能导致模型利用与稳定神经生理学无关的特异性（如解剖结构、设备噪声、头动伪影等“捷径”），使得高准确率并不等同于捕捉到了真实的、稳定的生物学特征，从而影响模型的泛化能力和生物学解释性。

-   **整体含义**：
    -   作者提出，理想的神经生理特征分析框架应在**不依赖参与者标签**的前提下，学习大脑活动的内在生成结构，让个体区分能力作为自然涌现的结果，而非直接优化目标。
    -   本研究旨在证明，**无监督的、与参与者无关的表示学习**是一种可扩展、可解释且能提取稳定神经生理特征的新范式。

### 2. 方法论

-   **核心思想**：
    -   采用**自编码器**架构，其训练目标仅为**重建输入的MEG时间序列片段**。模型被强迫压缩和还原信号，从而必须以潜在空间编码数据中的稳定、重复出现的结构。作者假设，这种结构包含了稳定的个体特异性信息，而瞬时状态变化会在时间平均中被抑制。
    -   **参与者无关性**：在整个模型训练（预训练和微调）过程中，**完全不使用任何参与者身份标签**。特征的个体区分能力是在特征提取和统计检验阶段自然涌现的。

-   **关键技术细节与流程**：
    1.  **信号预处理与源重建**：
        -   将静息态MEG传感器数据投影到皮层源空间，并划分为200个皮层感兴趣区域。
        -   为评估对解剖信息的鲁棒性，源重建分为两种条件：①**原生解剖**：使用参与者自己的MRI；②**代理解剖**：所有参与者共用一个“留出”参与者的MRI进行源重建。
    2.  **时间窗口划分**：将源空间时间序列切割为6秒长的非重叠窗口，作为模型的输入。
    3.  **自编码器架构**：
        -   **编码器**：由堆叠的2D卷积模块、群归一化、SiLU激活函数和Dropout组成，并在特定分辨率层引入单头自注意力机制。通过各向异性步长卷积进行下采样。编码器输出参数化一个对角高斯分布的潜在空间。
        -   **解码器**：与编码器对称，通过线性插值和卷积进行上采样，从潜在向量中重建原始的MEG窗口。
        -   **训练目标**：标准的VAE目标函数，即**均方误差重建损失 + KL散度正则化**。
    4.  **个体特征构建**：
        -   对于每个被试，在其连续数据上滑动窗口，每个窗口通过编码器得到一个潜在向量。
        -   **时间平均**：对属于同一被试的多个窗口的潜在向量进行平均，得到该被试的个体神经生理特征。
    5.  **个体区分**：通过计算不同数据段（如同一会话开始和结尾，或不同会话）的特征向量之间的**皮尔逊相关系数**来构建相似性矩阵，最高相关项在对角线上即为识别正确。

-   **基准方法**：
    -   **无模型方法**：①功能连接。②功率谱密度。
    -   **基于模型的有监督方法**：对比编码器，采用孪生网络架构和余弦嵌入损失，直接拉近同一参与者的嵌入、推远不同参与者的嵌入。

### 3. 实验设计

-   **数据集与场景**：
    -   **Cam-CAN**: 静息态MEG数据，闭眼状态，599名参与者，用于测试会话内区分及解剖鲁棒性、年龄/性别预测。
    -   **OMEGA**: 静息态MEG数据，多为睁眼状态，324名参与者（其中78名具有多次会话记录），用于测试跨会话泛化能力。

-   **关键评估基准与实验**：
    -   **会话内区分准确率与数据长度关系**：在10-120秒的不同记录时长下，评估并比较各方法的识别准确率。
    -   **对解剖信息的鲁棒性**：比较各方法在“原生解剖”和“代理解剖”条件下的表现，准确率下降越小代表鲁棒性越强。
    -   **跨会话泛化能力**：使用在单次会话数据上训练的模型，评估其在同一参与者的不同会话间进行个体识别的准确率，是检验特征稳定性的关键。
    -   **可解释性与敏感性分析**：利用解码器的生成能力，对潜在空间中主要成分进行置零，然后将向量解码回源空间，定量分析其对功能连接和功率谱等可解释特征的影响。
    -   **人口学变量预测**：使用个体特征预测年龄（偏最小二乘回归）和性别（逻辑回归），检验特征是否包含生物学相关信息。

-   **对比方法**：
    -   **无模型基线**：功能连接、功率谱密度。
    -   **有监督基线**：对比编码器。
    -   **自编码器变体**：①自编码器（预训练）；②微调的自编码器（在目标数据上以重建目标继续训练，作为上限对比）。

### 4. 资源与算力

-   **GPU型号与数量**：论文明确说明所有模型在**单块NVIDIA RTX 4080 SUPER GPU (16 GB显存)**上训练。
-   **训练时长**：
    -   每个自编码器完整训练约需**15小时**。
    -   对比编码器训练约需**11小时**。
-   **其他优化**：使用了梯度检查点技术以减少显存占用；模型使用AdamW优化器，批大小为4。

### 5. 实验数量与充分性

-   **实验数量与多样性**：实验设计相当全面，涉及了性能、鲁棒性、泛化性、可解释性等多个维度。
    -   **关键性能评估**：详尽评估了在不同数据长度（1s增量，共115个点）下的准确率。通过**100次自助抽样**进行统计检验和置信区间估计，并通过**Bonferroni校正**控制多重比较，确保了统计的严谨性。
    -   **泛化性与鲁棒性**：在两个独立的大规模数据集上进行测试，并专门评估了跨解剖条件和跨会话的性能，这是对模型能力的强有力检验。
    -   **机制与对比**：不仅与无模型和有监督基线进行对比，还拓展到年龄/性别预测、潜在空间分析（如ICC、PCA、几何距离）、以及对可解释特征的扰动敏感性分析，从多个角度验证了方法的有效性和特性。
-   **公平性分析**：代理解剖条件的设计，旨在分离神经活动与解剖伪影，为评估不同方法是否依赖“解剖捷径”提供了公平的基准。跨会话测试则检验了特征是否依赖会话特异性的稳定伪影。

### 6. 主要结论与发现

-   **无监督特征高度可区分且高效**：在代理解剖条件下，自编码器特征在**120秒**记录时达到**93.3%** 的识别准确率，在**14秒**时即可超越所有基线方法。
-   **对解剖信息具有高鲁棒性**：在代理解剖条件下，自编码器性能仅下降约**6.7个百分点**，远低于基准方法的12.6-19.8个百分点的下降，表明其更少依赖解剖特异性。
-   **跨会话泛化能力**：在OMEGA数据集上，自编码器和功能连接基线的跨会话准确率相当（约**50%**），远高于随机水平，而有监督的对比编码器性能则大幅崩溃（从100%降至13%），证明了无监督方法对真实、稳定神经特征的捕捉能力。
-   **具备生物学关联性**：自编码器特征预测年龄的精度（`r²=0.318`）显著优于所有基线方法。
-   **可解释性强**：通过解码器扰动分析，证明潜在空间的主成分能以结构化、可重复的方式映射回功能连接和频谱等已知神经生理特征空间。

### 7. 优点

-   **方法学创新**：首次将完全参与者无关的无监督自编码器框架成功应用于MEG时间序列的个体特征分析，明确挑战并规避了监督学习中潜在的“捷径学习”问题。
-   **评估全面且严谨**：实验设计周密，覆盖了性能效率、解剖和会话鲁棒性、可解释性和生物学预测等多个核心方面，统计检验规范。
-   **高数据效率与鲁棒性**：仅需十几秒的MEG数据即可超越传统方法，且对缺少个体MRI结构像的情况展现出很强的鲁棒性，具有实际应用潜力。
-   **架起可解释性的桥梁**：利用生成式解码器进行扰动分析，为通常被视为“黑箱”的深度学习模型提供了一种直接与神经生理学解释相联系的途径。

### 8. 不足与局限

-   **计算成本偏高**：与无模型基线相比，模型的训练和推理需要更多的算力和数据，这可能限制其在资源有限场景下的应用。
-   **高频信息表征不佳**：自编码器对高频活动（~30-45 Hz 伽马频段）的重建能力较弱，表明模型可能丢失了这些频段的信息。
-   **对非神经伪影的潜在敏感性**：尽管跨会话测试提供了证据，但仍无法完全排除模型利用了稳定的非神经伪影（如微小的头部位移结构），这些因素未被完全控制。
-   **跨站点和跨协议泛化需验证**：研究未在不同扫描设备、站点或预处理流程之间交叉验证，因此其在这些更具挑战性的泛化场景中的鲁棒性仍有待证明。
-   **可解释性的间接性**：扰动分析仍然是一种间接的黑箱探索方法，并未对潜在变量本身给出机械论层面的解释。

（完）
