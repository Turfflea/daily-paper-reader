---
title: "Generalized Linear Graph Representation: A Compact Operator Space for Graph Signal Processing and Graph Neural Networks"
title_zh: 广义线性图表示：图信号处理与图神经网络的紧致算子空间
authors: "Feiyue Zhao, Zhichao Zhang, Yangfan He"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14107v1"
tags: ["query:mt"]
score: 9.0
evidence: 提出新型紧凑双参数算子族用于图表示，改善GNN和图信号处理的关系数据建模。
tldr: "在图信号处理（GSP）和图神经网络（GNN）中，图拓扑的矩阵表示是核心，但现有参数化算子族扩展到转移型算子时变得不紧致，严重限制了理论分析和稳定学习。为此，本文提出广义线性图表示（GLGR）Q_{α,l}，作为定义在有界线性域上的紧致双参数算子族，统一了主要经典算子与转移型算子，无需渐近参数。理论上证明了其变分分解平衡局部平滑和全局度加权能量，推导了谱扰动界与正半定充分条件，并开发了自适应GLGR卷积（AG-Conv），使传播算子可端到端学习。实验表明，GLGR在多个骨干上提升了固定算子表示搜索和自适应图学习的性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14107v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14107v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1881, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14107v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 924, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14107v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1864, \"height\": 1129, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14107v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1865, \"height\": 1238, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14107v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1881, \"height\": 727, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14107v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1879, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14107v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1873, \"height\": 900, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14107v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1873, \"height\": 1008, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14107v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1868, \"height\": 420, \"label\": \"Table\"}]"
motivation: 现有图算子表示扩展到转移型算子时变得不紧致，限制理论分析与稳定学习。
method: "提出广义线性图表示（GLGR）Q_{α,l}，一个紧致双参数算子族，统一主要经典与转移型算子；开发自适应卷积AG-Conv，使传播算子可学习。"
result: 在图分类和节点分类多个基准上，GLGR提升了固定算子表示搜索和自适应图学习的性能，且跨多种骨干网络。
conclusion: GLGR为图处理提供了统一紧致算子空间，平衡局部平滑与全局能量，理论保证下提升了图学习性能。
---

## 摘要
图信号处理(GSP)与图神经网络(GNN)从根本上依赖于底层图拓扑的矩阵表示。这种表示定义了关键算子，如图傅里叶变换、谱滤波和卷积。现有的参数化算子族仅插值了经典图矩阵的部分子集，而更广泛的公式在表示转移型算子时变得非紧致，限制了理论分析和稳定学习。为解决这一问题，我们提出广义线性图表示(GLGR)，记作$\mathbf{Q}_{α,l}$，它是一个定义在有界线性域上的紧致两参数算子族。GLGR统一了主要经典算子与转移型算子，无需渐近参数。从理论上，我们证明$\mathbf{Q}_{α,l}$容许一种平衡局部平滑性和全局度加权能量的变分分解，推导出谱扰动界，并建立了正半定的图感知充分条件。基于该公式，我们开发了自适应GLGR卷积(AG-Conv)，使得传播算子本身可在端到端GNN中学习。在图分类和节点分类基准上的实验表明，GLGR在多种骨干网络上提升了固定算子表示搜索和自适应图学习的性能。

## Abstract
Graph Signal Processing (GSP) and Graph Neural Networks (GNNs) rely fundamentally on the matrix representation of the underlying graph topology. This representation defines key operators such as the graph Fourier transform, spectral filtering, and convolution. Existing parameterized operator families interpolate only partial subsets of classical graph matrices, while broader formulations become non-compact when representing transition-type operators, limiting both theoretical analysis and stable learning. To address this issue, we propose the Generalized Linear Graph Representation (GLGR), denoted by $\mathbf{Q}_{α,l}$, as a compact two-parameter operator family defined on a bounded linear domain. GLGR unifies major classical operators together with transition-type operators without requiring asymptotic parameters. Theoretically, we show that $\mathbf{Q}_{α,l}$ admits a variational decomposition balancing local smoothness and global degree-weighted energy, derive spectral perturbation bounds, and establish graph-aware sufficient conditions for positive semi-definiteness. Building on this formulation, we develop Adaptive GLGR Convolution (AG-Conv), which makes the propagation operator itself learnable within end-to-end GNNs. Experiments on graph classification and node classification benchmarks show that GLGR improves both fixed-operator representation search and adaptive graph learning across multiple backbones.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

- **研究背景与动机**：图信号处理（GSP）和图神经网络（GNN）的性能高度依赖于底层图拓扑的矩阵表示（如图拉普拉斯矩阵或邻接矩阵），该表示决定了图傅里叶变换、谱滤波和卷积等核心操作。
- **现存问题**：经典算子（如邻接矩阵、拉普拉斯矩阵）及现有参数化算子族（如 \( \mathbf{A}_\alpha \), \( \mathbf{L}_\alpha \), \( \mathbf{P}_{\alpha,k} \)）存在碎片化问题，无法在一个紧凑的、有界的参数空间内统一表示所有关键算子。特别是，当表示“转移型算子”（如 \( \mathbf{T}_\alpha = \alpha\mathbf{D} + (1-2\alpha)\mathbf{A} \)）时，先前的 \( \mathbf{P}_{\alpha,k} \) 族需要参数 \( k \to \infty \)，导致非紧致性，严重阻碍了理论分析和基于梯度的稳定学习。
- **整体含义**：本文旨在构建一个紧致、统一且适合优化的图算子空间，从根本上解决表示碎片化和非紧致性问题，从而同时提升图信号处理的谱分析能力和图神经网络的可学习性。

### 2. 论文提出的方法论

- **核心思想**：提出**广义线性图表示（GLGR）**，即算子族 \( \mathbf{Q}_{\alpha,l} = \alpha\mathbf{D} + (1-\alpha-l)\mathbf{A} \)。通过线性解耦对角（度矩阵）和非对角（邻接矩阵）分量，并引入一个独立的偏移参数 `l`，实现在一个紧凑的二维有界域 \( (\alpha, l) \in [0,1] \times [0,2] \) 内平滑控制不同的结构先验。
- **关键技术细节**：
    - **算子统一性**：该定义在紧凑域内无缝覆盖了所有经典算子（如 \( \mathbf{A} \), \( \mathbf{D} \), \( \mathbf{L} \), \( \mathbf{Q}_{SL} \)）和参数族路径（\( \mathbf{A}_\alpha \), \( \mathbf{L}_\alpha \)），特别是**转移型算子 \( \mathbf{T}_\alpha \) 可通过简单的线性路径 \( l = \alpha \) 实现**，无需参数趋向无穷。
    - **变分与谱理论**：
        - **能量分解**：证明了二次型 \( \mathbf{x}^\top \mathbf{Q}_{\alpha,l} \mathbf{x} \) 可分解为局部平滑项（狄利克雷能量）和全局度加权能量的线性组合，系数由 \( \alpha \) 和 \( l \) 控制。
        - **谱扰动稳定性**：推导了在拓扑扰动下 \( \mathbf{Q}_{\alpha,l} \) 特征值的利普希茨连续性和扰动上界，其敏感系数 \( K = \alpha\sqrt{N} + |1-\alpha-l| \) 是有限的，而对比之下 \( \mathbf{P}_{\alpha,k} \) 的敏感系数随 \( k\to\infty \) 而发散。
        - **正半定性条件**：给出了一个图感知的、确保矩阵正半定的充分条件，为核方法等应用提供了理论指导。
    - **可学习算子（AG-Conv）**：开发了**自适应GLGR卷积（AG-Conv）**，将 \( \alpha^{(k)} \) 和 \( l^{(k)} \) 设为每层可学习的参数，通过Sigmoid函数约束在域内。该层可表示为多项式滤波器 \( \mathbf{H}^{(k+1)} = \Phi\left(\sum_{p=0}^{K} (\mathbf{Q}^{(k)})^p \mathbf{H}^{(k)} \mathbf{\Theta}_p^{(k)}\right) \)，使传播算子的频谱响应能根据任务自适应调整，包括平滑、锐化和带通等模式。

### 3. 实验设计

论文主要采用了两条互补的实验路径，分别验证 GLGR 的表示能力和可学习性。

- **实验一：图分类（固定算子表示验证）**
    - **数据集与场景**：使用了 TUDataset 中的 **12个图分类基准数据集**，涵盖生物化学和社交网络领域（如 PROTEINS, ENZYMES, IMDB-BINARY 等）。
    - **方法论与对比**：构建了一个基于 GLGR 谱相关性（SCor）的核SVM分类管线。在 \( (\alpha, l) \) 平面进行穷举网格搜索，绘制精度热力图，并将其与使用纯邻接矩阵或拉普拉斯矩阵等**经典算子的性能进行对比**。实验对比了 GLGR 最优区域与先前 \( \mathbf{P}_{\alpha,k} \) 族边界之外（GLGR独占区域）的表现。

- **实验二：节点分类（可学习算子验证）**
    - **数据集与场景**：使用了 **13个节点分类基准数据集**，包括7个同配图（如 Cora, PubMed, Photo）和6个异配图（如 Texas, Cornell, Actor）。数据规模覆盖广泛。
    - **基线方法**：将 AG-Conv 作为即插即用的传播算子，集成到 **8种代表性GNN骨干网络**中，包括 SGC, GCN, GPRGNN, APPNP, ChebNet, BernNet, JacobiConv 和 SGFormer。
    - **对比方式**：进行严格的成对比较。对于每种骨干网络，在**完全相同的超参数设置下**（继承自公开基准代码），训练原始模型和其 GLGR 增强版本，并报告在10次随机划分上的平均精度±标准差。此外，还与更广泛的非GLGR方法（如 GAT, H2GCN, GraphGPS, NodeFormer 等）进行了列表对比。

### 4. 资源与算力

- **说明与算力细节**：论文在 "Baselines and Settings" 部分明确指出，所有实验在配备 **单块 NVIDIA RTX 3060 GPU** 和 Intel i5-12400F CPU 的工作站上运行。实现的软件环境为 **Python 3.9.12** 和 **PyTorch 1.11.0+cu113**。文中未提供具体的总训练时长，但说明了详细的硬件配置，具备复现的基本信息。

### 5. 实验数量与充分性

- **实验数量**：实验规模庞大。图分类部分涉及12个数据集上的 \( 21 \times 11 = 231 \) 个网格点的穷举搜索。节点分类部分包含 8 种骨干网络在 13 个数据集上的性能对比（共计 104 组成对实验），每项实验又基于 10 次随机划分。此外，还提供了跨数据集和骨干网络的频谱响应可视化，以及与其他9种SOTA模型的综合比较。
- **充分性与公平性评估**：
    - **充分性**：实验设计系统且全面，从表示空间搜索到端到端学习，覆盖了同配/异配、不同规模的图，验证了方法的双重价值。
    - **客观性与公平性**：节点分类实验的公平性尤为突出，采用“固定默认超参数”的严格对比协议，避免了为自身方法进行额外超参数调优带来的优势，所得增益更具说服力。图分类实验中固定 SVM 的 C 和 γ 参数，保证了对比聚焦于表征本身。

### 6. 论文的主要结论与发现

- **紧致统一性**：GLGR 成功在一个有界线性域内统一了离散的经典和参数化图算子族，并解决了先前方法表示转移型算子时的非紧致性（渐近参数）问题。
- **理论优越性**：GLGR 具有清晰的能量分解解释，并且在面对图拓扑扰动时，其谱稳定性显著优于非紧致的 \( \mathbf{P}_{\alpha,k} \) 族，为稳定学习提供了理论依据。
- **表示空间增益**：固定算子的图分类实验表明，最优性能往往出现在纯经典算子之外的 GLGR 区域，甚至位于先前参数族的边界之外，证明了 GLGR 确实拓展了有用的谱搜索空间。
- **可学习性与泛化性**：在节点分类任务中，即插即用的 AG-Conv 能够在多种骨干网络和同配/异配数据集上**一致地提升性能**，特别是在异配图上提升显著，证明了其能够通过端到端学习自适应用户任务的最优频谱响应，并具有良好的跨架构泛化能力。

### 7. 优点

- **方法设计简洁优美**：提出的双参数线性算子 \( \mathbf{Q}_{\alpha,l} \) 形式简单，参数空间直观（矩形域），却解决了根本性的非紧致问题，数学美感强。
- **理论与实践并重**：论文不仅有严谨的变分分解、扰动界和正半定性等理论支撑，还开发了 AG-Conv 将其无缝落地到深度学习，并证明了其梯度优化稳定性优于前作。
- **实验设计极为扎实**：采用双实验轨道分别验证表示能力和可学习性。节点分类实验坚持“**不改动基线超参数**”的公平对比原则，结果更具可信度，有力证明了方法的普适增益。
- **架构无关性**：AG-Conv 实现为一种即插即用的传播算子模块，可轻松集成到消息传递、谱方法、扩散模型和图Transformer等多种类型的骨干网络中，实用性强。

### 8. 不足与局限

- **理论泛化限制**：正半定性条件目前是**充分非必要的保守条件**，对一般图的精确定义依赖于具体的图拓扑，文中仅为正则图提供了可视化示例。谱扰动界也采用了图无关的保守估计。
- **未充分探索规范化算子**：GLGR 的当前定义基于非规范化的组合拉普拉斯算子 \( \mathbf{D} - \mathbf{A} \)。对于 GNN 中普遍采用的对称规范化算子，该方法如何等效应用或会遇到何种新问题，论文未深入探讨。
- **实验覆盖的深度**：虽然图分类实验探索了算子空间，但其下游任务管线相对单一（仅使用 SCOR+SVM），未测试该算子与其他图核或更先进GNN图池化框架结合的效果。
- **参数初始化敏感性**：文中未讨论可学习参数 \( \alpha, l \) 的初始化策略对训练稳定性和收敛速度的影响，这在实践中可能是一个关键点。
- **谱响应分析**：AG-Conv 学习到的频谱响应虽然被可视化，但其与数据集特性（如同配率、特征分布）的关联分析仍停留在定性层面，缺乏更深入的归因研究。

（完）
