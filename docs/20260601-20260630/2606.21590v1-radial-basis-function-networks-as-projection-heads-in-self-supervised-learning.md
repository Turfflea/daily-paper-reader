---
title: Radial Basis Function Networks as Projection Heads in Self-Supervised Learning
title_zh: 自监督学习中作为投影头的径向基函数网络
authors: "Andreas Schliebitz, Heiko Tapken, Martin Atzmueller"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21590v1"
tags: ["query:mt"]
score: 9.0
evidence: 径向基函数网络作为自监督学习投影头，配合无标签质量度量
tldr: 自监督学习通常依赖骨干编码器和小型MLP投影头，训练后投影头被丢弃，转而通过昂贵的线性探测评估骨干质量，造成计算浪费。提出用径向基函数网络(RBFN)作为投影头，利用其可解释的中心和形状参数设计无标签尺度归一化分离度(SNS)度量。跨五种经典SSL架构和四个图像分类数据集实验表明，RBFN头可竞争替代MLP头，推荐三层高斯径向基函数；SNS与逻辑回归指标呈强正相关。该工作还贡献了基于Google Open Images V7的PyTorch分类数据集，促进表示学习可复现研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21590v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 428, \"height\": 624, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21590v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 423, \"height\": 466, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21590v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 661, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21590v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1252, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21590v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1253, \"height\": 400, \"label\": \"Table\"}]"
motivation: 自监督学习中MLP投影头训练后被丢弃，线性探测评估骨干质量昂贵且计算浪费。
method: 提出用RBFN替换MLP头，利用其可解释核参数引出无标签质量度量SNS。
result: RBFN头在多种SSL架构上达到竞争性能，SNS与逻辑回归指标强正相关，可代理表示质量。
conclusion: RBFN投影头是高效替代方案，SNS无需标签即可评估，并发布了新数据集。
---

## 摘要
自监督学习通常依赖一个骨干编码器，后接一个小型多层感知机投影头，该投影头在训练后通常被丢弃，而骨干网络的质量则通过在有标签数据上进行昂贵的线性探测来评估。我们认为这种做法包括丢弃投影头，在计算上相当浪费。取而代之，我们提出用径向基函数网络替换多层感知机头，其可解释的中心和形状参数可用于在没有标签或单独分类器的情况下判断表示质量。为此，我们引入了尺度归一化分离，这是一种新颖的无标签质量度量，完全从训练过程中学习到的核中心和形状推导而来。在五种经典的自监督学习架构和四个图像分类数据集上，我们表明径向基函数网络投影头可以作为标准多层感知机投影头的有竞争力即插即用替代品。我们建议使用由高斯径向基函数激活的三层径向基函数结构来构建它们。此外，尺度归一化分离与已有的逻辑回归度量标准表现出强到非常强的正相关性，表明经过训练的径向基函数网络投影头可以作为骨干表示质量的可靠代理。我们还发布了一个基于Google Open Images V7的新的PyTorch兼容图像分类数据集，以促进表示学习的可重复研究。

## Abstract
Self-supervised learning (SSL) typically relies on a backbone encoder followed by a small multilayer perceptron (MLP) projection head, which is conventionally discarded after training, while backbone quality is assessed via costly linear probing on labeled data. We argue that this approach including discarding the projector is rather computationally wasteful. Instead, we propose replacing the MLP head with a radial basis function network (RBFN), whose interpretable center and shape parameters can be exploited to judge representation quality without labels or a separate classifier. To this end, we introduce Scale-Normalized Separation (SNS), a novel label-free quality metric derived solely from the kernel centers and shapes learned during training. Across five canonical SSL architectures (MoCo, SimCLR, BYOL, SwAV and SimSiam) and four image classification datasets, we show that RBFN projection heads are competitive drop-in replacements for standard MLP projectors. We recommend constructing them with three RBF layers activated by the Gaussian radial basis function. Moreover, SNS exhibits strong to very strong positive correlation with established logistic regression metrics, demonstrating that a trained RBFN projector can act as a reliable proxy for backbone representation quality. We additionally publish a novel PyTorch compatible image classification dataset based on Google's Open Images V7 to facilitate reproducible research into representation learning.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将基于提供的论文内容，为您生成一份结构化、深入、客观的中文总结。

### **1. 核心问题与整体含义（研究动机和背景）**

*   **核心问题**：当前自监督学习（SSL）的标准实践中，训练完成后通常无条件地丢弃投影头，转而通过在有标签数据上训练线性分类器（逻辑回归）来评估骨干网络的质量。这在计算和信息理论上都是一种浪费。
*   **研究动机**：探索能否用一种新型的、具有更强可解释性的非线性投影头来代替传统的MLP，并利用其学习到的参数，在不依赖有标签数据和下游任务的情况下，直接评估骨干网络的表示质量。
*   **整体含义**：本研究旨在证明，通过使用径向基函数网络作为投影头，不仅可以保持甚至提升模型性能，还能从其“中心”和“形状”参数中衍生出一个无标签的质量度量（SNS），作为骨干网络质量的可靠代理，从而简化SSL的评价流程，减少对标注数据的依赖。

### **2. 方法论：核心思想与关键技术细节**

*   **核心思想**：
    *   **RBFN投影头替代MLP**：用具有高度可解释性的径向基函数网络替换自监督学习架构（如SimCLR、MoCo）中的标准MLP投影头。
    *   **基于投影头参数的无标签评估**：假设骨干网络学习的聚类质量可以通过RBFN投影头学到的可解释参数（核中心`c`和形状`ε`）来表征，从而在不依赖标签的情况下判断表示质量。

*   **关键技术细节**：
    *   **RBFN投影头结构**：
        *   其设计遵循SimCLR的MLP投影头样式，由线性层、批量归一化和RBF层组合而成。
        *   新的关键超参数包括：径向基函数类型（推荐`高斯`）、核数量`K`（默认2048）、层数（推荐3层）。
        *   基本单元结构：`输入 (2048维)` → `线性层` → `批量归一化(可选)` → `RBF层(含K个核)`。可通过堆叠该单元来增加网络深度。输出通常为128维。
        *   对于需要预测头的架构（如BYOL, SimSiam），提供了相应的单层RBFN预测头。
    *   **尺度归一化分离（SNS）度量**：
        *   **出发点**：衡量学到的RBF核在投影空间中的分离程度。核间距与核尺度匹配得越好，表示聚类质量可能越高。
        *   **计算流程**：
            1.  **尺度归一化距离 (`δ_ij`)**：衡量任意两个不同的核中心`c_i`, `c_j`之间的距离, 并除以它们的联合尺度 (`sqrt(ε_i^2 + ε_j^2)`), 同时用`√D` (D为特征维度) 进行维度校正。
            2.  **高斯相互作用函数 (`ψ(δ_ij)`)**： `δ_ij^2 * exp(-δ_ij^2)`。该函数在 `δ_ij=1` 时能量达到最大，在`δ_ij`趋近于0或无穷大时趋于零。
            3.  **最终SNS得分**：所有无序核对的平均相互作用能量。SNS最大值意味着所有核之间的尺度归一化距离趋近于1，即核间距与核尺度匹配良好。

### **3. 实验设计**

*   **数据集/场景**：
    *   主要基准：**ImageNet-100** (100类)。
    *   类别数量变化实验：从Google的 **Open Images V7** 数据集采样的三个子集，包含 **10、30和50** 个类别。这些子集与ImageNet-1K不相交，并具有更高的平均图像分辨率。
    *   所有数据集均按70/10/20的比例划分为训练、验证和测试集。
*   **Benchmark与对比方法**：
    *   **基准模型**：使用默认MLP投影头的五种经典SSL架构：**MoCo (v2), SimCLR, BYOL, SwAV, SimSiam**。所有架构均使用在ImageNet-1K上预训练的ResNet-50作为骨干网络。
    *   **提出的方法**：将上述五种架构的默认MLP投影头替换为本文提出的**RBFN投影头**。
    *   **评估协议**：遵循标准协议，自监督训练完成后，在有标签的测试集上训练逻辑回归模型，并报告准确率（A）、精确率（P）、召回率（R）和F1分数。
    *   **对比验证**：将RBFN头与默认MLP头（并测试了增加MLP至3或4层的情况）的性能进行对比。
    *   **新度量验证**：计算每次RBFN训练的SNS得分，并将其与逻辑回归指标进行皮尔逊和斯皮尔曼相关性分析。

### **4. 资源与算力**

*   **硬件配置**：所有训练运行在**8块NVIDIA A100 80 GB GPU**上。
*   **训练模式**：采用分布式数据并行（DDP）模式。
*   **训练时长**：
    *   基线训练：未明确提及单次运行时长。
    *   RBFN训练：根据架构不同，每次训练运行大约持续**1.5至2.5小时**。

### **5. 实验数量与充分性**

*   **实验数量**：
    *   **基线实验**：在4个数据集上，针对MoCo和SimCLR调整投影头深度（2, 3, 4层），共进行了**36次**训练和评估。
    *   **RBFN实验**：对超参数进行网格搜索，包括：核类型（4种）、核数量（3种）、网络层数、是否归一化，覆盖5种架构和4个数据集，共进行了**864次**训练和评估。
*   **实验充分性评估**：
    *   **充分性**：实验设计较为全面。覆盖了多个主流的对比式和非对比式SSL框架，在多个不同规模的数据集上进行了验证。对RBFN超参数进行了详尽的消融研究，并使用SHAP分析来量化各超参数的影响。
    *   **客观与公平性**：对比时，除投影头外，所有架构、数据集、优化器和训练轮数等设置均保持一致，保证了比较的公平性。相关性分析进一步从统计学上验证了新度量的有效性。因此，实验整体上是充分、客观且公平的。

### **6. 主要结论与发现**

1.  **RBFN投影头是有竞争力的替代品**：在多个SSL架构和数据集上，RBFN投影头与标准MLP投影头的性能相当，差异在误差范围内，验证了其作为即插即用组件的可行性。
2.  **推荐RBFN结构配置**：基于SHAP分析，推荐使用**3层、由高斯径向基函数激活、不做归一化**的RBFN投影头。核数量的影响不显著，但建议与投影空间维度（如128）相匹配。
3.  **SNS度量是表示质量的有效代理**：SNS得分与通过逻辑回归评估获得的所有四个分类指标（A, P, R, F1）均表现出强到非常强的正相关性（皮尔逊相关系数在0.79到0.93之间）。这表明，仅通过分析训练好的RBFN投影头参数，就可以可靠地评估骨干网络的表示质量，而无需额外的有标签数据和下游任务训练。
4.  **新的数据集贡献**：发布的基于Open Images V7的子集为研究表示学习在类别数量变化下的行为提供了一个有价值的、与ImageNet不相交的基准。

### **7. 优点**

*   **创新性方案**：首次将具有强可解释性归纳偏置的RBFN引入SSL主流框架作为投影头，为解决投影头利用不充分的问题提供了新思路。
*   **高效的无标签评估**：提出的SNS度量直接从训练过程中产生的参数计算得到，避免了耗时的线性探测步骤和对标注数据的依赖，显著提升了SSL模型评估的效率。
*   **即插即用与兼容性**：RBFN投影头被设计为与现有主流SSL框架（特别是LightlySSL）兼容的即插即用替代方案，易于集成和使用。
*   **扎实的实证分析**：通过大规模的网格搜索、SHAP模型解释和统计相关性分析，为RBFN的有效性和SNS度量的可靠性提供了强有力的证据。

### **8. 不足与局限**

*   **性能上的微小差距**：尽管总体性能相当，但在部分数据集（如OpenImagesV7-30/50）上，RBFN投影头的峰值性能略逊于最优的默认MLP头（约1-3%的准确率差距）。
*   **SNS度量的泛化性**：SNS度量的有效性是在RBFN投影头的语境下验证的。如果投影头结构发生根本性变化，该度量可能不再适用。
*   **骨干网络和数据集限制**：所有实验均基于ResNet-50骨干网络和图像分类任务。结论在其他类型的骨干网络（如Vision Transformer）或任务（如语义分割、目标检测）上的泛化性有待进一步验证。
*   **超参数搜索范围**：虽然RBFN超参数网格搜索已相当详尽，但对核数量这一参数的探索（128, 256, 512）可能尚未覆盖最佳范围，尤其是考虑远小于128的核时的情况。
*   **SNS只适用于训练后**：SNS与逻辑回归性能相关，但无法提供绝对的质量阈值来区分“好”与“差”的模型，其应用目前仍限于在相同设置下的模型间进行比较和选择。

（完）
